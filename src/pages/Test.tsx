import { Template } from '@/components'
import { FC } from 'react'

interface TestProps {}

export const Test: FC = () => {
    return (
        <Template>
            <h1>Test</h1>
        </Template>
    )
}
