import { FC } from 'react'
import { Template } from '@/components'

interface ConfigProps {}

export const Config: FC = () => {
    return (
        <Template>
            <h1>Config</h1>
        </Template>
    )
}

